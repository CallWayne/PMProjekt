<%= form_for(effort_category) do |f| %>
  <% if effort_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(effort_category.errors.count, "error") %> prohibited this effort_category from being saved:</h2>

      <ul>
      <% effort_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %>
    <%= f.number_field :value %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
